import React, { Component } from 'react';
import './Logout.css';

class Logout extends Component {
    render() {
        return (
            <div class="logout">
                <h1>Logout</h1>
            </div>
        )
    }
}

export default Logout;